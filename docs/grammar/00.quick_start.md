# 00 Quick Start

30 分钟内跑通 Yian 的核心语法。每个示例均可编译运行。

编译方式：
- **单文件**：`python3 -m compiler.main lib <file>`
- **多文件项目**：使用 anx 包管理器（`python3 -m anx.main build`）

## 1. Hello World

```yian
// hello.an
fn main() -> i32 {
    return 0;
}
```

```bash
python3 -m compiler.main lib hello.an && ./build/a.out
```

## 2. 变量与类型

```yian
fn main() -> i32 {
    let x: i32 = 42;
    let y = x + 1;              // 类型推断
    let flag: bool = true;
    let c: char = 'A';
    assert y == 43;
    return 0;
}
```

来源：`tests/array/assign.an`

## 3. 函数

```yian
fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn main() -> i32 {
    let result: i32 = add(1, 2);
    assert result == 3;
    return 0;
}
```

## 4. 控制流

```yian
fn main() -> i32 {
    let x: i32 = 10;

    if x > 0 {
        return 1;
    } elif x < 0 {
        return -1;
    } else {
        return 0;
    }
}
```

## 5. for-in 循环

```yian
fn main() -> i32 {
    let sum: i32 = 0;
    for i in 0..10 {
        sum += i;
    }
    assert sum == 45;           // -- 来源: tests/control_flow/for.an
    return 0;
}
```

## 6. 数组

```yian
fn main() -> i32 {
    let arr: i32[3] = [1, 2, 3];   // -- 来源: tests/array/assign.an
    arr[0] = 10;
    assert arr[0] == 10;
    return 0;
}
```

## 7. 结构体

```yian
struct Point {
    x: i32,
    y: i32,
}

fn main() -> i32 {
    let p: Point = Point(3, 4);
    assert p.x == 3;
    assert p.y == 4;
    return 0;
}
```

## 8. 枚举与 match

```yian
from std.core.option import Option;

fn main() -> i32 {
    let opt: Option<i32> = Option<i32>.Some(42);

    let val: i32 = match opt {
        Option::Some(v) => v,
        Option::None => 0,
    };
    assert val == 42;           // -- 来源: tests/std/core/option.an
    return 0;
}
```

## 9. Vec 动态数组

```yian
from std.core.vec import Vec;

fn main() -> i32 {
    let v: Vec<i32> = Vec<i32>.new();
    v.push(10);
    v.push(20);
    v.push(30);
    assert v.len() == 3;        // -- 来源: tests/std/core/vec/main.an
    assert v[0] == 10;
    return 0;
}
```

## 10. String

```yian
from std.core.string import String;

fn main() -> i32 {
    let s: String = String.from(&"Hello");
    s.push_str(" World");
    s += "!";
    assert s == "Hello World!"; // -- 来源: tests/std/core/string.an
    return 0;
}
```

## 11. 泛型

```yian
fn first<T>(a: T, b: T) -> T {
    return a;
}

fn main() -> i32 {
    let x: i32 = first(1, 2);   // -- 来源: tests/generics/func.an
    assert x == 1;
    return 0;
}
```

## 12. 运算符重载

```yian
from std.core.ops import Add;

struct Value<T> { pub value: T }

impl<T> Add<Value<T>, Value<T>> for Value<T> {
    fn add(rhs: Self) -> Self {
        return Self(self.value + rhs.value);
    }
}

fn main() -> i32 {
    let a: Value<i32> = Value<i32>(10);
    let b: Value<i32> = Value<i32>(20);
    assert (a + b).value == 30;     // -- 来源: tests/std/core/ops/arith.an
    return 0;
}
```

## 13. 模块导入

```yian
from std.core.vec import Vec;
from std.core.string import String;

fn main() -> i32 {
    let v: Vec<i32> = Vec<i32>.new();
    let s: String = String.new();
    return 0;
}
```

## 14. 包管理（anx）

对于多文件项目，使用 anx 包管理器：

```bash
# 创建项目
python3 -m anx.main new myapp
cd myapp

# 创建库并配置依赖
python3 -m anx.main new --lib mylib
# 编辑 myapp/package.anx，在 [dependencies] 中声明 mylib = { path = "../mylib" }

# 构建并运行
python3 -m anx.main run
```

anx 项目使用包名导入替代相对路径：

```yian
from myapp.utils.math import add;      // 同包导入
from mylib.strings import join;        // 依赖包导入
from std.core.io import print;         // stdlib
```

## 下一步

- [§01 词法](01.lexicon.md) — 标识符、关键字、字面量
- [§02 类型系统](02.types.md) — 完整类型参考
- [§04 函数](04.functions.md) — 函数定义、参数、返回值
- [§06 控制流](06.control_flow.md) — 全部控制流结构
- [§14 标准库](14.standard_library.md) — Option、Result、Vec、String 等
