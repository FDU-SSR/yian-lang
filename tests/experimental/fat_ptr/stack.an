fn main() -> i32 {
    let a: i32 = 6
    let fpa: i32* = &a
    let res: i32 = 0
    res += fpa[0]
    res += *fpa
    fpa[0] = res
    *fpa = res + 1
    assert res == 12: "test 1 fail"
    assert a == 13: "test 2 fail"
    return 0
}