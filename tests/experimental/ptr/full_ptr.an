from ptr import FullPtr;
fn main() {
    let p: FullPtr<i32> = FullPtr<i32>.new_object()
    *p = 100
    let v: i32 = *p
    assert v == 100: "fail 1"
    p.realloc(10)
    p[3] = 5
    p[6] = 8
    assert p[3] == 5: "fail 2"
    assert p[6] == 8: "fail 3"
    return 0
}