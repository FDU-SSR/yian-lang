// Enums support ordinary assignment.
enum Color { Red, Green, Blue }

fn main() {
    let c1 = Color.Red;
    let c2 = c1;
    match c1 { Red => {}, _ => panic("source changed") }
    match c2 { Red => {}, _ => panic("copy changed") }
}
