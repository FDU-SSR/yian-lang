// Structs with user-defined fields can be copied.
struct Data { value: i64 }

impl Clone for Data {
    pub fn clone() -> Self { Data(self.value) }
}

struct Bad { x: i32, d: Data }

fn main() {
    let b1 = Bad(1, Data(42));
    let b2 = b1;
    assert b1.d.value == 42 && b2.d.value == 42;
}
