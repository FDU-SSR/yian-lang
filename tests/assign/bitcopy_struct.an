// Test: @BitCopy struct supports direct assignment
//
// Expected output: 0

@BitCopy
struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p1 = Point(1, 2);
    let p2 = p1;  // ok: Point is @BitCopy
    assert p2.x == 1: "x should be 1";
    assert p2.y == 2: "y should be 2";
}
