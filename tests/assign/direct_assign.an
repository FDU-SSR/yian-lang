// User-defined values can be assigned directly.
struct BigInt { value: i64 }

impl Clone for BigInt {
    pub fn clone() -> Self { BigInt(self.value) }
}

fn main() {
    let a: BigInt = BigInt(1);
    let c = a;
    assert a.value == 1 && c.value == 1;
}
