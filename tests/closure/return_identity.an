fn identity<F>(f: F) -> F {
    return f;
}

fn main() -> i32 {
    let x: i32 = 10;
    let closure = |x_in = x| (y: i32) -> i32 {
        x_in + y
    };
    let same = identity(closure);
    let result = same(5);
    assert result == 15: "identity pass-through";
    return 0;
}
