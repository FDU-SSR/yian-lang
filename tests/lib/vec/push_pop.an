from std.core.vec import Vec

i32 main() {
    Vec<i32> v = Vec<i32>.with_capacity(10)
    v.push(1)
    v.push(2)
    v.push(3)
    v.push(4)
    v.push(5)

    i32 sum = 0
    while v.len() > 0 {
        sum += v.pop().unwrap()
    }

    assert sum == 15: "Expected sum to be 15"

    return 0
}