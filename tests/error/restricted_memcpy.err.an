// Test that the `__memcpy` builtin is rejected outside the standard
// library.
//
// Expected compilation error: restricted operation

fn main() {
    let a: i32 = 1;
    let b: i32 = 2;
    __memcpy(&b, &a, 4);
}
