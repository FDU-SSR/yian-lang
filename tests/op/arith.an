// Test arithmetic operations: +, -, *, /, %
//
// Expected output: 0

i32 main() {
    i32 a = 10
    i32 b = 3

    i32 sum = a + b
    assert sum == 13: "Addition failed"

    i32 diff = a - b
    assert diff == 7: "Subtraction failed"

    i32 prod = a * b
    assert prod == 30: "Multiplication failed"

    i32 quot = a / b
    assert quot == 3: "Division failed"

    i32 rem = a % b
    assert rem == 1: "Modulus failed"

    return 0
}