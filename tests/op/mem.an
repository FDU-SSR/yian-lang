// Test memory operations: dereference and address-of
//
// Expected output: 0

fn main() {
    let a: i32 = 42;
    let p: i32* = &a;  // Address-of operator

    assert *p == 42: "Dereference failed";  // Dereference operator

    *p = 100;
    assert a == 100: "Memory write via pointer failed";

}