// Basic expression-oriented test
// Tests that the existing code patterns still work

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn main() {
    let x: i32 = add(1, 2);
    assert x == 3: "basic add failed";
}
