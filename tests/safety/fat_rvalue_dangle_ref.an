// A temporary reference must carry the current frame lock as well.

fn make() -> i32& {
    &(42i32)
}

fn main() {
    let r: i32& = make();
    let x: i32 = *r;
    assert x == 42: "rvalue reference should not escape";
}
