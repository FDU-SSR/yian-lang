// Test out-of-bounds array access handling
//
// Expected runtime error: Index out of bounds

fn main() {
    let arr: i32[3] = [1, 2, 3];
    let idx: u64 = 3;
    let err: i32 = arr[idx]  // OOB access;
}