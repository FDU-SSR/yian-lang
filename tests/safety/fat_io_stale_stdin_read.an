// Stdin.read must validate a stale slice before entering the read syscall.

from std.core.io import Stdin;

fn main() {
    let bytes: u8* = dyn[4] u8;
    let stale: u8[] = bytes[0u64..4];
    del bytes;
    Stdin.read(stale);
}
