// Taking the address of a temporary must bind it to the current frame lock.

fn make() -> i32* {
    &(42i32)
}

fn main() {
    let p: i32* = make();
    let x: i32 = *p;
    assert x == 42: "rvalue address should not escape";
}
