// Writing a stale str through the public print boundary must fail closed.

from std.core.io import print;

fn main() {
    let p: u8* = dyn[4] 0u8;
    p[0] = 65u8;
    p[1] = 66u8;
    p[2] = 67u8;
    p[3] = 10u8;
    let text: str = str.from(&p[0u64..4]);
    del p;
    print(text);
}
