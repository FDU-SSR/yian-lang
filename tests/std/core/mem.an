// Test mem functions
//
// Expected output: 0

from std.core.mem import memcpy;

fn main() {
    let src = [1u8, 2, 3, 4, 5];
    let dest = [0u8; 5];

    // Test memcpy function
    memcpy(dest.ptr(), src.ptr(), 5);
    assert dest[0] == 1;
    assert dest[4] == 5;

}