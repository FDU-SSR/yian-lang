// Closure parse test: omitted -> void return type.
//
// Expected output: 0

fn main() {
    let x: i32 = 0;
    let c = |x_ptr = &x| (delta: i32) {
        *x_ptr = *x_ptr + delta;
    };
    c(5);
    assert x == 5: "closure with void return";
}
