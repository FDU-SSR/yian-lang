// Test that the removed `from_raw_parts` wrapper cannot be imported by user code.

from std.core.slice import from_raw_parts as forge;

fn main() {
    let x: i32 = 1;
    let p: i32* = &x;
    let s: i32[] = forge(p, 1);
}
