// A released RawVec cannot be revived by shrink_to_fit().
from std.core.raw_vec import RawVec;

fn main() {
    let values = RawVec<i32>.with_capacity(4);
    values.drop();
    values.shrink_to_fit(0);
}
