// Test struct construction and field access
//
// Expected output: 0

struct Person {
    pub age: i32,
    pub weight: float,
}

fn main() {
    let p: Person = Person(20, 60.5);
    let age: i32 = p.age;
    assert age == 20: "Person struct construction failed";
    if p.weight > 61.0 {
        panic("Person weight field access failed");
    }
}