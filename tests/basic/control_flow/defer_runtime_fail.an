from std.core.io import Stderr;

fn main() {
    Stderr.write_all("before\n").unwrap();
    defer Stderr.write_all("deferred\n").unwrap();
    let values: i32[1] = [1];
    let value: i32 = values[1u64];
}
