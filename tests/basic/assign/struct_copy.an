// Test: structs support ordinary assignment
//
// Expected output: 0

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p1 = Point(1, 2);
    let p2 = p1;
    assert p2.x == 1: "x should be 1";
    assert p2.y == 2: "y should be 2";
    assert p1.x == 1: "source remains readable";
}
