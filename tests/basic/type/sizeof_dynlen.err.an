// sizeof(i32[n]) where n is a runtime variable — array length must be a
// compile-time constant.  n is a local let-binding, not a const generic in
// the unit's global symbol context.

fn main() {
    let n: u64 = 4;
    let s: u64 = sizeof(i32[n]);
}
