// Test match statement with char type
//
// Expected output: 0

fn main() -> i32 {
    let a: char = 'b';
    match a {
        'a' => { panic("Matched a"); },
        'b' => { return 0; },
        'c', 'd' => { panic("Matched c || d"); },
        _ => { panic("Matched default case"); },
    }
}
