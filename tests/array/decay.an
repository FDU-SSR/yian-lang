// Array decay: T[N]→T* and T[N]*→T*
fn main() {
    let arr: u64[4] = [10, 20, 30, 40];
    let p: u64* = arr;       // T[N]→T* (array decay)
    assert *p == 10;
    assert p[1] == 20;
    let q: u64* = &arr;      // T[N]*→T* (re-anchor)
    assert *q == 10;
    q += 1;
    assert *q == 20;
}
