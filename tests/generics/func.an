// Test generic functions
//
// Expected output: 0

fn add<T>(a: T, b: T) -> T {
    return a + b;
}

fn abs<T>(a: T) -> T {
    if (a < 0) {
        return -a;
    }
    return a;
}

fn main() -> i32 {
    // 1) explicit type arguments
    let x: i32 = add<i32>(10, 20);
    assert x == 30: "Expected 30";
    let y: i32 = abs<i32>(-5);
    assert y == 5: "Expected 5";

    // 2) type inference
    let m: f64 = add(1.5, 2.5);
    assert m > 3.9 && m < 4.1: "Expected 4.0";
    let n: f64 = abs(-3.2);
    assert n > 3.1 && n < 3.3: "Expected 3.2";

    return 0;
}