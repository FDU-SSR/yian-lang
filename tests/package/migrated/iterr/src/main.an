// Importing a non-pub typedef must fail.
// Expected compilation error: Symbol 'Secret' exists in the imported unit but is not declared 'pub'

from tderrtest.secret import Secret

fn main() {
    let x: Secret = 1
    return 0
}
