from std.core.hash import Murmur3Hasher, Hash;

impl Hash<Murmur3Hasher> for i16 {
    fn hash(hasher: Murmur3Hasher) {
        let value: i16 = *self;
        let bytes: u8[2] = [0, 0];
        __memcpy(&bytes, &value, 2);
        hasher.write(bytes.as_slice());
    }
}
