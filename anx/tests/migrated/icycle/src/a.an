// lib file in circular import cycle

from cyctest.main import unwrap_value;

@BitCopy
pub struct Value<T> {
    pub value: T,
}

pub fn lib_unwrap_value<T>(v: Value<T>) -> T {
    return unwrap_value(v);
}

